# this will pass phase 2 and phase 3
# if stmt that has an else must return in both, and any elifs for the if stmt to be returnable

def foo[x:Integer]:Integer{
    if[x > 10]{
	    return 1;
	}
	else{
	    return 0;
    }
}

def main[]:Void{
    ::print[foo[10]];
}