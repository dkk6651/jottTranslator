jottTranslator contains the program's main function. It accepts three arguments: the input Jott file, the output
file for the translated code, and the language of desired translation.

Usage: jottTranslator <input> <output> <language>
You MUST specify all three arguments
<input>     The Jott file to be translated
<output>    The destination of the translation
<language>  The desited language of translation
            Can either be "Jott", "Java", "Python", or "C"